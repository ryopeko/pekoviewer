%h1 Listing activities

%table.table.table-striped.table-hover
  %tr
    %th Actor screen name
    %th Source type
    %th Description
    %th Created at

  - @activities.each do |activity|
    %tr
      %td= activity.actor_screen_name
      %td
        = service_image_tag activity
        = activity.source_type
      %td= (activity.permalink.present? ? (link_to activity.description, activity.permalink) : activity.description)
      %td= activity.created_at

%div
  = paginate @activities

